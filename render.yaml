services:
  - type: web
    name: diabetes-cluster-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
    healthCheckPath: /
    autoDeploy: true 